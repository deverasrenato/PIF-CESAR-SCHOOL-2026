#include <stdio.h>    // Biblioteca padrão para entrada e saída (permite usar o printf)
#include <stdlib.h>   // Biblioteca de utilidades gerais (contém a função system)

int main() {
    // Ponto de partida da execução do programa
    
    prinf("Primeiro programa! :)"); // Exibe a mensagem inicial na tela (atenção ao erro de digitação original!)
    
    system("pause"); // Pausa a execução para o terminal não fechar automaticamente
    
    return 0;        // Indica que o programa terminou com sucesso
} 

// Fim do código principal
